extends Node

# TODO: Find where is the location of Mario Multiverse's game exe's server
	  # may require some hacking to do so, ONLY for educational purproses api key 
	  # isn't needed surpsrisingly :)

func _ready() -> void:
	return
